{
  "name": "memoires-website",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for Memoires, an iPhone journalling app.",
  "scripts": {
    "dev": "tailwindcss -i src/input.css -o assets/tailwind.css --watch",
    "build": "tailwindcss -i src/input.css -o assets/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
